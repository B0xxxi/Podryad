<!DOCTYPE html>
<html lang="ru" data-theme>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= city.name %></title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="container">
    <h1><%= city.name %></h1>
    <p><strong>DDX:</strong> <%= city.ddx ? 'Да' : 'Нет' %></p>
    <p><strong>МЮЗ:</strong> <%= city.myuz ? 'Да' : 'Нет' %></p>
    <h2>Монтажники:</h2>
    <% if (city.Installers.length) { %>
      <ul>
        <% city.Installers.forEach(inst => { %>
          <li><%= inst.name %></li>
        <% }) %>
      </ul>
    <% } else { %>
      <p>Нет данных.</p>
    <% } %>
    <h2>Сисадмины:</h2>
    <% if (city.Sysadmins.length) { %>
      <ul>
        <% city.Sysadmins.forEach(sa => { %>
          <li><%= sa.name %></li>
        <% }) %>
      </ul>
    <% } else { %>
      <p>Нет данных.</p>
    <% } %>
    <div class="links">
      <a href="/search">Назад к поиску</a>
      <a href="/logout">Выход</a>
      <a href="/admin">Админ-панель</a>
    </div>
    <button id="theme-toggle">Переключить тему</button>
  </div>
  <script src="/js/theme.js"></script>
</body>
</html> 