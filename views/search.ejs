<!DOCTYPE html>
<html lang="ru" data-theme>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Поиск по городу</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/common.css">
  <link rel="stylesheet" href="/css/search.css">
  <link rel="stylesheet" href="/css/custom-buttons.css">
</head>
<body>
  <div class="main-container">
    <h1 class="search-container__title">Поиск по городу</h1>
    <form action="/search" method="GET" class="search-form">
      <input type="text" name="q" placeholder="Введите город" class="search-form__input" value="<%= q || '' %>" required />
      <button type="submit" class="button button--primary button--light">Найти</button>
    </form>
    <% if (q) { %>
      <ul class="results">
        <% if (cities.length) { %>
          <% cities.forEach(city => { %>
            <li class="results__item"><a href="/city/<%= city.id %>" class="results__link"><%= city.name %></a></li>
          <% }) %>
        <% } else { %>
          <li class="results__item">Ничего не найдено.</li>
        <% } %>
      </ul>
    <% } %>
    <div class="links">
      <a href="/logout" class="button button--secondary">Выход</a>
      <a href="/admin" class="button button--secondary">Админ-панель</a>
    </div>
    <button id="theme-toggle" class="button button--secondary">Переключить тему</button>
  </div>
  <script src="/js/theme.js"></script>
</body>
</html> 